# Layer 2 — Reasoning Traces / 推論軌跡

Design dialogues that produced this Formation, as Markdown + JSON pairs, dated `YYYY-MM-DD_slug`. **CC0.** A reader who replays one of these dialogues with their own language model collaborator should arrive somewhere recognizably equivalent — that reproducibility is the point. / 本Formationを生んだ設計対話。Markdown＋JSONペア、`YYYY-MM-DD_slug`命名。**CC0。** これらの対話を自身の言語モデル協働者と再演した読者は、認識可能な程度に等価な地点へ到達するはずである——その再現性こそが眼目である。

## Inaugural traces (in preparation) / 最初の軌跡（準備中）

`2026-07-12_ptsg-cpu-birth-from-wpms-formation-session` (.md/.json) — **“The Five Impossibilities — the pre-launch council of PTSG-CPU-Formation / 五つの不可能——PTSG-CPU-Formation設立前会議.”** The slug deliberately rhymes with the Core's own birth trace, `ptsg-birth-from-wpms-session`: the three-generation recursion — WPMS keeps birthing generic children and becomes each child's first customer — is thus legible from the Layer 2 inventory itself. / スラッグはCore自身の誕生トレースと意図的に韻を踏む: 三世代の再帰がLayer 2目録そのものから読める。

The session spans the Formation ISA draft (modes 1–3), the computability audit and its repository-informed corrections, the PTSG-CPU spin-off decision, the split-stack doctrine, the read-only quartet, the architect's rulings of 2026-07, and the Measured-not-promised governance rule — is being prepared as this inaugural trace pair. / ローンチセッション——Formation ISAたたき台（モード1〜3）、計算可能性監査とそのリポジトリ精読による訂正、PTSG-CPU暖簾分け決定、スタック二分教義、読出し専用カルテット、2026-07の設計者裁定群、実測主義統治規則——を最初の軌跡ペアとして準備中。

### Worksheets / 作業物

`_worksheets/` holds non-normative working artifacts; see its README for the inventory. The underscore prefix marks them as outside the md+json trace format. / `_worksheets/`は非規範の作業物を収める。目録は同READMEを参照。

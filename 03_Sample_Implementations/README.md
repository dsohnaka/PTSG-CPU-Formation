# Layer 3 — Sample Implementations / リファレンス実装

Reference implementations of PTSG-CPU-Formation. **MIT.** Illustrative, not normative — one possible implementation, not *the* implementation. / PTSG-CPU-Formationのリファレンス実装。**MIT。** 例示であって規範ではない——一つの可能な実装であり、唯一の実装ではない。

Planned contents: the Formation datapath RTL (issue-port decode, ALU/MAC, data stack, PPM pages with atomic commit, quartet taps), instruction-list examples (the Maclaurin exp-as-MAC-sequence demo among the first), and testbenches keyed to the Command × Phase rows. / 予定: Formationデータパス RTL、命令列例（マクローリンexpのMAC列デモを最初期に）、Command × Phase行に対応づけたテストベンチ。

To come. / 準備中。
